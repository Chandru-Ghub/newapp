import React from 'react'

const Apirequest = async(url,option,err) => {
  try{
        const response = await fetch(url).then(data.json()).then(result)
  }
  catch(error){
        
  }
}

export default Apirequest